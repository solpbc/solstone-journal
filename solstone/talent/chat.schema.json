{
  "type": "object",
  "additionalProperties": false,
  "required": ["message", "notes", "talent_request"],
  "properties": {
    "message": {"type": ["string", "null"]},
    "notes": {"type": "string"},
    "talent_request": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["target", "task", "context"],
      "properties": {
        "target": {"type": "string", "enum": ["exec", "reflection"]},
        "task": {"type": "string"},
        "context": {"type": ["string", "null"]}
      }
    }
  }
}
