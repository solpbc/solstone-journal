// SPDX-License-Identifier: AGPL-3.0-only
// Copyright (c) 2026 sol pbc

(function () {
  'use strict';

  // § 5 rung 2 — the system timezone's tzdb reference point. Bundled with the build: § 12
  // accepts no network call attributable to this pattern, so the table is never fetched.
  // Regenerate the block between the markers with:
  //   vpx/design-system/tools/gen-sun-arc-zone-points.py js-var --into <this file>
  // sun-arc-zone-points:begin — GENERATED by vpx/design-system/tools/gen-sun-arc-zone-points.py, do not hand-edit.
  // tzdb 2026c zone.tab, 418 zone identifiers. Rung 2 of the sun-arc fallback chain (spec § 5):
  // 'Region/City:lat:lon,City:lat:lon|Region/...', lat and lon in signed hundredths of a degree.
  var SUNARC_ZONE_COUNT = 418;
  var SUNARC_ZONE_TABLE =
    'Africa/Abidjan:532:-403,Accra:555:-22,Addis_Ababa:903:3870,Algiers:3678:305,Asmara:1533:3888,Bamako:1265:-800,Bangui:437:1858,Banjul:1347:-1665,Bissau:1185:-1558,Blantyre:-1578:3500,Brazzaville:-427:1528,Bujumbura:-338:2937,Cairo:3005:3125,Casablanca:3365:-758,Ceuta:3588:-532,Conakry:952:-1372,Dakar:1467:-1743,Dar_es_Salaam:-680:3928,Djibouti:1160:4315,Douala:405:970,El_Aaiun:2715:-1320,Freetown:850:-1325,Gaborone:-2465:2592,Harare:-1783:3105,Johannesburg:-2625:2800,Juba:485:3162,Kampala:32:3242,Khartoum:1560:3253,Kigali:-195:3007,Kinshasa:-430:1530,Lagos:645:340,Libreville:38:945,Lome:613:122,Luanda:-880:1323,Lubumbashi:-1167:2747,Lusaka:-1542:2828,Malabo:375:878,Maputo:-2597:3258,Maseru:-2947:2750,Mbabane:-2630:3110,Mogadishu:207:4537,Monrovia:630:-1078,Nairobi:-128:3682,Ndjamena:1212:1505,Niamey:1352:212,Nouakchott:1810:-1595,Ouagadougou:1237:-152,Porto-Novo:648:262,Sao_Tome:33:673,Tripoli:3290:1318,Tunis:3680:1018,Windhoek:-2257:1710|America/Adak:5188:-17666,Anchorage:6122:-14990,Anguilla:1820:-6307,Antigua:1705:-6180,Araguaina:-720:-4820,Argentina/Buenos_Aires:-3460:-5845,Argentina/Catamarca:-2847:-6578,Argentina/Cordoba:-3140:-6418,Argentina/Jujuy:-2418:-6530,Argentina/La_Rioja:-2943:-6685,Argentina/Mendoza:-3288:-6882,Argentina/Rio_Gallegos:-5163:-6922,Argentina/Salta:-2478:-6542,Argentina/San_Juan:-3153:-6852,Argentina/San_Luis:-3332:-6635,Argentina/Tucuman:-2682:-6522,Argentina/Ushuaia:-5480:-6830,Aruba:1250:-6997,Asuncion:-2527:-5767,Atikokan:4876:-9162,Bahia:-1298:-3852,Bahia_Banderas:2080:-10525,Barbados:1310:-5962,Belem:-145:-4848,Belize:1750:-8820,Blanc-Sablon:5142:-5712,Boa_Vista:282:-6067,Bogota:460:-7408,Boise:4361:-11620,Cambridge_Bay:6911:-10505,Campo_Grande:-2045:-5462,Cancun:2108:-8677,Caracas:1050:-6693,Cayenne:493:-5233,Cayman:1930:-8138,Chicago:4185:-8765,Chihuahua:2863:-10608,Ciudad_Juarez:3173:-10648,Costa_Rica:993:-8408,Coyhaique:-4557:-7207,Creston:4910:-11652,Cuiaba:-1558:-5608,Curacao:1218:-6900,Danmarkshavn:7677:-1867,Dawson:6407:-13942,Dawson_Creek:5577:-12023,Denver:3974:-10498,Detroit:4233:-8305,Dominica:1530:-6140,Edmonton:5355:-11347,Eirunepe:-667:-6987,El_Salvador:1370:-8920,Fort_Nelson:5880:-12270,Fortaleza:-372:-3850,Glace_Bay:4620:-5995,Goose_Bay:5333:-6042,Grand_Turk:2147:-7113,Grenada:1205:-6175,Guadeloupe:1623:-6153,Guatemala:1463:-9052,Guayaquil:-217:-7983,Guyana:680:-5817,Halifax:4465:-6360,Havana:2313:-8237,Hermosillo:2907:-11097,Indiana/Indianapolis:3977:-8616,Indiana/Knox:4130:-8662,Indiana/Marengo:3838:-8634,Indiana/Petersburg:3849:-8728,Indiana/Tell_City:3795:-8676,Indiana/Vevay:3875:-8507,Indiana/Vincennes:3868:-8753,Indiana/Winamac:4105:-8660,Inuvik:6835:-13372,Iqaluit:6373:-6847,Jamaica:1797:-7679,Juneau:5830:-13442,Kentucky/Louisville:3825:-8576,Kentucky/Monticello:3683:-8485,Kralendijk:1215:-6828,La_Paz:-1650:-6815,Lima:-1205:-7705,Los_Angeles:3405:-11824,Lower_Princes:1805:-6305,Maceio:-967:-3572,Managua:1215:-8628,Manaus:-313:-6002,Marigot:1807:-6308,Martinique:1460:-6108,Matamoros:2583:-9750,Mazatlan:2322:-10642,Menominee:4511:-8761,Merida:2097:-8962,Metlakatla:5513:-13158,Mexico_City:1940:-9915,Miquelon:4705:-5633,Moncton:4610:-6478,Monterrey:2567:-10032,Montevideo:-3491:-5621,Montserrat:1672:-6222,Nassau:2508:-7735,New_York:4071:-7401,Nome:6450:-16541,Noronha:-385:-3242,North_Dakota/Beulah:4726:-10178,North_Dakota/Center:4712:-10130,North_Dakota/New_Salem:4684:-10141,Nuuk:6418:-5173,Ojinaga:2957:-10442,Panama:897:-7953,Paramaribo:583:-5517,Phoenix:3345:-11207,Port-au-Prince:1853:-7233,Port_of_Spain:1065:-6152,Porto_Velho:-877:-6390,Puerto_Rico:1847:-6611,Punta_Arenas:-5315:-7092,Rankin_Inlet:6282:-9208,Recife:-805:-3490,Regina:5040:-10465,Resolute:7470:-9483,Rio_Branco:-997:-6780,Santarem:-243:-5487,Santiago:-3345:-7067,Santo_Domingo:1847:-6990,Sao_Paulo:-2353:-4662,Scoresbysund:7048:-2197,Sitka:5718:-13530,St_Barthelemy:1788:-6285,St_Johns:4757:-5272,St_Kitts:1730:-6272,St_Lucia:1402:-6100,St_Thomas:1835:-6493,St_Vincent:1315:-6123,Swift_Current:5028:-10783,Tegucigalpa:1410:-8722,Thule:7657:-6878,Tijuana:3253:-11702,Toronto:4365:-7938,Tortola:1845:-6462,Vancouver:4927:-12312,Whitehorse:6072:-13505,Winnipeg:4988:-9715,Yakutat:5955:-13973|Antarctica/Casey:-6628:11052,Davis:-6858:7797,DumontDUrville:-6667:14002,Macquarie:-5450:15895,Mawson:-6760:6288,McMurdo:-7783:16660,Palmer:-6480:-6410,Rothera:-6757:-6813,Syowa:-6901:3959,Troll:-7201:253,Vostok:-7840:10690|Arctic/Longyearbyen:7800:1600|Asia/Aden:1275:4520,Almaty:4325:7695,Amman:3195:3593,Anadyr:6475:17748,Aqtau:4452:5027,Aqtobe:5028:5717,Ashgabat:3795:5838,Atyrau:4712:5193,Baghdad:3335:4442,Bahrain:2638:5058,Baku:4038:4985,Bangkok:1375:10052,Barnaul:5337:8375,Beirut:3388:3550,Bishkek:4290:7460,Brunei:493:11492,Chita:5205:11347,Colombo:693:7985,Damascus:3350:3630,Dhaka:2372:9042,Dili:-855:12558,Dubai:2530:5530,Dushanbe:3858:6880,Famagusta:3512:3395,Gaza:3150:3447,Hebron:3153:3509,Ho_Chi_Minh:1075:10667,Hong_Kong:2228:11415,Hovd:4802:9165,Irkutsk:5227:10433,Jakarta:-617:10680,Jayapura:-253:14070,Jerusalem:3178:3522,Kabul:3452:6920,Kamchatka:5302:15865,Karachi:2487:6705,Kathmandu:2772:8532,Khandyga:6266:13555,Kolkata:2253:8837,Krasnoyarsk:5602:9283,Kuala_Lumpur:317:10170,Kuching:155:11033,Kuwait:2933:4798,Macau:2220:11354,Magadan:5957:15080,Makassar:-512:11940,Manila:1459:12097,Muscat:2360:5858,Nicosia:3517:3337,Novokuznetsk:5375:8712,Novosibirsk:5503:8292,Omsk:5500:7340,Oral:5122:5135,Phnom_Penh:1155:10492,Pontianak:-3:10933,Pyongyang:3902:12575,Qatar:2528:5153,Qostanay:5320:6362,Qyzylorda:4480:6547,Riyadh:2463:4672,Sakhalin:4697:14270,Samarkand:3967:6680,Seoul:3755:12697,Shanghai:3123:12147,Singapore:128:10385,Srednekolymsk:6747:15372,Taipei:2505:12150,Tashkent:4133:6930,Tbilisi:4172:4482,Tehran:3567:5143,Thimphu:2747:8965,Tokyo:3565:13974,Tomsk:5650:8497,Ulaanbaatar:4792:10688,Urumqi:4380:8758,Ust-Nera:6456:14323,Vientiane:1797:10260,Vladivostok:4317:13193,Yakutsk:6200:12967,Yangon:1678:9617,Yekaterinburg:5685:6060,Yerevan:4018:4450|Atlantic/Azores:3773:-2567,Bermuda:3228:-6477,Canary:2810:-1540,Cape_Verde:1492:-2352,Faroe:6202:-677,Madeira:3263:-1690,Reykjavik:6415:-2185,South_Georgia:-5427:-3653,St_Helena:-1592:-570,Stanley:-5170:-5785|Australia/Adelaide:-3492:13858,Brisbane:-2747:15303,Broken_Hill:-3195:14145,Darwin:-1247:13083,Eucla:-3172:12887,Hobart:-4288:14732,Lindeman:-2027:14900,Lord_Howe:-3155:15908,Melbourne:-3782:14497,Perth:-3195:11585,Sydney:-3387:15122|Europe/Amsterdam:5237:490,Andorra:4250:152,Astrakhan:4635:4805,Athens:3797:2372,Belgrade:4483:2050,Berlin:5250:1337,Bratislava:4815:1712,Brussels:5083:433,Bucharest:4443:2610,Budapest:4750:1908,Busingen:4770:868,Chisinau:4700:2883,Copenhagen:5567:1258,Dublin:5333:-625,Gibraltar:3613:-535,Guernsey:4945:-254,Helsinki:6017:2497,Isle_of_Man:5415:-447,Istanbul:4102:2897,Jersey:4918:-211,Kaliningrad:5472:2050,Kirov:5860:4965,Kyiv:5043:3052,Lisbon:3872:-913,Ljubljana:4605:1452,London:5151:-13,Luxembourg:4960:615,Madrid:4040:-368,Malta:3590:1452,Mariehamn:6010:1995,Minsk:5390:2757,Monaco:4370:738,Moscow:5576:3762,Oslo:5992:1075,Paris:4887:233,Podgorica:4243:1927,Prague:5008:1443,Riga:5695:2410,Rome:4190:1248,Samara:5320:5015,San_Marino:4392:1247,Sarajevo:4387:1842,Saratov:5157:4603,Simferopol:4495:3410,Skopje:4198:2143,Sofia:4268:2332,Stockholm:5933:1805,Tallinn:5942:2475,Tirane:4133:1983,Ulyanovsk:5433:4840,Vaduz:4715:952,Vatican:4190:1245,Vienna:4822:1633,Vilnius:5468:2532,Volgograd:4873:4442,Warsaw:5225:2100,Zagreb:4580:1597,Zurich:4738:853|Indian/Antananarivo:-1892:4752,Chagos:-733:7242,Christmas:-1042:10572,Cocos:-1217:9692,Comoro:-1168:4327,Kerguelen:-4935:7022,Mahe:-467:5547,Maldives:417:7350,Mauritius:-2017:5750,Mayotte:-1278:4523,Reunion:-2087:5547|Pacific/Apia:-1383:-17173,Auckland:-3687:17477,Bougainville:-622:15557,Chatham:-4395:-17655,Chuuk:742:15178,Easter:-2715:-10943,Efate:-1767:16842,Fakaofo:-937:-17123,Fiji:-1813:17842,Funafuti:-852:17922,Galapagos:-90:-8960,Gambier:-2313:-13495,Guadalcanal:-953:16020,Guam:1347:14475,Honolulu:2131:-15786,Kanton:-278:-17172,Kiritimati:187:-15733,Kosrae:532:16298,Kwajalein:908:16733,Majuro:715:17120,Marquesas:-900:-13950,Midway:2822:-17737,Nauru:-52:16692,Niue:-1902:-16992,Norfolk:-2905:16797,Noumea:-2227:16645,Pago_Pago:-1427:-17070,Palau:733:13448,Pitcairn:-2507:-13008,Pohnpei:697:15822,Port_Moresby:-950:14717,Rarotonga:-2123:-15977,Saipan:1520:14575,Tahiti:-1753:-14957,Tarawa:142:17300,Tongatapu:-2113:-17520,Wake:1928:16662,Wallis:-1330:-17617';
  // sun-arc-zone-points:end

  let zoneTable = null;

  // Unpack the table above into { 'Region/City': { lat, lon } }.
  function parseZoneTable(encoded) {
    const out = Object.create(null);
    const blocks = encoded.split('|');
    for (let i = 0; i < blocks.length; i += 1) {
      const cut = blocks[i].indexOf('/');
      const region = blocks[i].slice(0, cut);
      const entries = blocks[i].slice(cut + 1).split(',');
      for (let j = 0; j < entries.length; j += 1) {
        const parts = entries[j].split(':');
        out[region + '/' + parts[0]] = { lat: Number(parts[1]) / 100, lon: Number(parts[2]) / 100 };
      }
    }
    return out;
  }

  // The zone's reference point, or null for an identifier the table does not know (a legacy
  // alias with no row) — the caller then takes rung 3.
  function zonePoint(zoneId) {
    if (typeof zoneId !== 'string' || !zoneId) return null;
    if (!zoneTable) zoneTable = parseZoneTable(SUNARC_ZONE_TABLE);
    return zoneTable[zoneId] || null;
  }

  // The device's own zone identifier, or null where Intl is unavailable (then rung 3).
  function systemZoneId() {
    try {
      if (typeof Intl === 'undefined' || typeof Intl.DateTimeFormat !== 'function') return null;
      const resolved = Intl.DateTimeFormat().resolvedOptions();
      return (resolved && resolved.timeZone) || null;
    } catch (e) {
      return null;
    }
  }

  function parseNumeric(val) {
    if (typeof val === 'number') {
      return Number.isFinite(val) ? val : null;
    }
    if (typeof val !== 'string') return null;
    val = val.trim();
    if (!val) return null;
    if (val.endsWith('%')) {
      const num = parseFloat(val.slice(0, -1));
      return Number.isFinite(num) ? num / 100 : null;
    }
    if (val.endsWith('deg')) {
      const num = parseFloat(val.slice(0, -3));
      return Number.isFinite(num) ? num : null;
    }
    const num = parseFloat(val);
    return Number.isFinite(num) ? num : null;
  }

  function validateColor(raw) {
    if (typeof raw !== 'string') return null;
    raw = raw.trim();
    if (!raw) return null;
    const lower = raw.toLowerCase();
    if (['inherit', 'initial', 'unset', 'revert', 'revert-layer', 'none'].includes(lower)) {
      return null;
    }
    if (typeof CSS !== 'undefined' && typeof CSS.supports === 'function') {
      if (!CSS.supports('color', raw)) return null;
    }
    if (typeof document !== 'undefined' && document.createElement) {
      const probe = document.createElement('span');
      probe.style.color = 'rgb(1, 2, 3)';
      probe.style.color = raw;
      if (!probe.style.color) return null;
      if (probe.style.color === 'rgb(1, 2, 3)' && lower !== 'rgb(1, 2, 3)' && lower !== 'rgb(1,2,3)') {
        return null;
      }
    }
    return raw;
  }

  function parseTokens(source) {
    let getProp = null;
    if (source && typeof source.getPropertyValue === 'function') {
      getProp = (name) => source.getPropertyValue(name);
    } else if (source && source.nodeType === 1 && typeof window !== 'undefined' && window.getComputedStyle) {
      const style = window.getComputedStyle(source);
      getProp = (name) => style.getPropertyValue(name);
    } else if (source && typeof source === 'object') {
      getProp = (name) => source[name];
    } else if (typeof document !== 'undefined' && document.documentElement && typeof window !== 'undefined' && window.getComputedStyle) {
      const style = window.getComputedStyle(document.documentElement);
      getProp = (name) => style.getPropertyValue(name);
    } else {
      return { valid: false };
    }

    const raw = {
      angle: parseNumeric(getProp('--sunarc-angle')),
      bowRatio: parseNumeric(getProp('--sunarc-bow-ratio')),
      diameterRatio: parseNumeric(getProp('--sunarc-diameter-ratio')),
      cornerOvershoot: parseNumeric(getProp('--sunarc-corner-overshoot')),
      envelopeEdge: parseNumeric(getProp('--sunarc-envelope-edge')),
      peakOpacity: parseNumeric(getProp('--sunarc-peak-opacity')),
      peakOpacityDark: parseNumeric(getProp('--sunarc-peak-opacity-dark')),
      twilightMinutes: parseNumeric(getProp('--sunarc-twilight-minutes')),
      glowRadiusRatio: parseNumeric(getProp('--sunarc-glow-radius-ratio')),
      glowDayAlpha: parseNumeric(getProp('--sunarc-glow-day-alpha')),
      glowMidStop: parseNumeric(getProp('--sunarc-glow-mid-stop')),
      glowMidRatio: parseNumeric(getProp('--sunarc-glow-mid-ratio')),
      twilightRadiusRatio: parseNumeric(getProp('--sunarc-twilight-radius-ratio')),
      twilightAlphaDark: parseNumeric(getProp('--sunarc-twilight-alpha-dark')),
      twilightAlphaLight: parseNumeric(getProp('--sunarc-twilight-alpha-light')),
      twilightSink: parseNumeric(getProp('--sunarc-twilight-sink')),
      trueDarkMinutes: parseNumeric(getProp('--sunarc-true-dark-minutes')),
      glowColor: validateColor(getProp('--sunarc-glow-color')),
      twilightDarkTo: validateColor(getProp('--sunarc-twilight-dark-to')),
      twilightLightFrom: validateColor(getProp('--sunarc-twilight-light-from')),
      groundLightDay: validateColor(getProp('--sunarc-ground-light-day')),
      groundLightNight: validateColor(getProp('--sunarc-ground-light-night')),
      groundLightDeep: validateColor(getProp('--sunarc-ground-light-deep')),
      groundDarkDay: validateColor(getProp('--sunarc-ground-dark-day')),
      groundDarkNight: validateColor(getProp('--sunarc-ground-dark-night')),
      groundDarkDeep: validateColor(getProp('--sunarc-ground-dark-deep')),
      // The 09-19 derivation inputs. The engine no longer reads them; the smoke page keeps
      // them as the oracle that the dark night ground is still the spec's own mix.
      mixInk: parseNumeric(getProp('--sunarc-mix-ink')),
      mixWarm: parseNumeric(getProp('--sunarc-mix-warm')),
      warmDark: validateColor(getProp('--sunarc-warm-dark')),
      nightGround: validateColor(getProp('--sunarc-night-ground')),
      creamBright: validateColor(getProp('--cream-bright')),
      ink: validateColor(getProp('--ink')),
    };

    return raw;
  }

  function tokensValid(tokens) {
    if (!tokens || typeof tokens !== 'object') return false;
    const requiredNumbers = [
      'angle', 'bowRatio', 'diameterRatio', 'cornerOvershoot', 'envelopeEdge',
      'peakOpacity', 'peakOpacityDark', 'twilightMinutes', 'glowRadiusRatio',
      'glowDayAlpha', 'glowMidStop', 'glowMidRatio', 'twilightRadiusRatio',
      'twilightAlphaDark', 'twilightAlphaLight', 'twilightSink', 'trueDarkMinutes'
    ];
    for (const key of requiredNumbers) {
      if (typeof tokens[key] !== 'number' || !Number.isFinite(tokens[key])) {
        return false;
      }
    }
    const requiredColors = [
      'glowColor', 'twilightDarkTo', 'twilightLightFrom',
      'groundLightDay', 'groundLightNight', 'groundLightDeep',
      'groundDarkDay', 'groundDarkNight', 'groundDarkDeep'
    ];
    for (const key of requiredColors) {
      if (!validateColor(tokens[key])) {
        return false;
      }
    }
    return true;
  }

  function geometry(W, H, t, tokens) {
    const side = Math.min(W, H);
    const diameterRatio = tokens.diameterRatio;
    const diameter = diameterRatio * side;
    const R = diameter / 2;
    const k = tokens.cornerOvershoot;
    const delta = (k * R) / Math.SQRT2;

    const A = { x: -delta, y: -delta };
    const B = { x: W + delta, y: H + delta };
    const dx = B.x - A.x;
    const dy = B.y - A.y;
    const c = Math.hypot(dx, dy);
    const s = tokens.bowRatio * c;
    const Rarc = (c * c) / (8 * s) + s / 2;

    const M = { x: (A.x + B.x) / 2, y: (A.y + B.y) / 2 };
    const ux = dx / c;
    const uy = dy / c;
    // Perpendicular pointing toward larger y (bottom of viewport)
    let vx = -uy;
    let vy = ux;
    if (vy < 0) {
      vx = -vx;
      vy = -vy;
    }

    const O = {
      x: M.x + (Rarc - s) * vx,
      y: M.y + (Rarc - s) * vy,
    };

    const thetaA = Math.atan2(A.y - O.y, A.x - O.x);
    const thetaB = Math.atan2(B.y - O.y, B.x - O.x);
    const angleDeg = 2 * Math.asin(c / (2 * Rarc)) * (180 / Math.PI);

    const theta = thetaA + (thetaB - thetaA) * t;
    const pos = {
      x: O.x + Rarc * Math.cos(theta),
      y: O.y + Rarc * Math.sin(theta),
    };

    const thetaMid = (thetaA + thetaB) / 2;
    const center = {
      x: O.x + Rarc * Math.cos(thetaMid),
      y: O.y + Rarc * Math.sin(thetaMid),
    };

    return {
      side,
      diameter,
      R,
      A,
      B,
      c,
      s,
      Rarc,
      O,
      center,
      pos,
      angleDeg,
    };
  }

  function env(t, e) {
    if (t < 0 || t > 1) return 0;
    if (e <= 0) return 1;
    let val;
    if (t < e) {
      val = Math.sin((t / e) * (Math.PI / 2));
    } else if (t <= 1 - e) {
      val = 1;
    } else {
      val = Math.sin(((1 - t) / e) * (Math.PI / 2));
    }
    return Math.max(0, Math.min(1, val));
  }

  function nightAmount(m, rise, set, tw) {
    const dawn = rise - tw;
    const dusk = set + tw;
    if (m < dawn) return 1;
    if (m < rise) return (rise - m) / tw;
    if (m <= set) return 0;
    if (m <= dusk) return (m - set) / tw;
    return 1;
  }

  function dayProgress(m, rise, set, tw) {
    const dawn = rise - tw;
    const dusk = set + tw;
    return (m - dawn) / (dusk - dawn);
  }

  // The sun's opacity: the plateau peak for the ground it sits on (0.55 on a light ground,
  // 0.20 on a dark one, spec § 8a), eased by the envelope and faded through twilight.
  function sunOpacity(params) {
    const { t, env: envVal, nightAmount: nightAmt, peakOpacity } = params;
    if (t < -0.02 || t > 1.02) return 0;
    return peakOpacity * envVal * (1 - nightAmt);
  }

  // Holds near 1, then eases to 0 at x = 1.
  function twilightEase(x) {
    const c = Math.max(0, Math.min(1, x));
    return Math.pow(1 - c * c, 1.5);
  }

  // § 4a — half the true-dark window for a night of `span` minutes (dusk to the next dawn): up
  // to half of trueDarkMinutes, but the glow always keeps at least an hour to ease out and an
  // hour to ease in, so a short night gets a shorter true dark and a white night (span <= 120)
  // none. A port of SUNARC.deepHalf.
  function trueDarkHalf(span, trueDarkMinutes) {
    return Math.max(0, Math.min(trueDarkMinutes, span - 120)) / 2;
  }

  // § 4a — the twilight weight w and the hidden sun's extended parameter te. By day w = 1 − env;
  // after dusk it eases out to the true-dark window, before dawn it eases back in. x is how far
  // into the evening (or how close to the dawn) we are, 0 → 1. A port of SUNARC.twilight; m, rise
  // and set are on frame()'s one extended minute axis.
  function twilightWeight(m, rise, set, envVal, t, tw, trueDarkMinutes, sink) {
    const dawn = rise - tw;
    const dusk = set + tw;
    if (m >= dawn && m <= dusk) return { w: 1 - envVal, te: t, x: 0, phase: 'day' };
    const span = wrap(dawn - dusk, 1440);
    const ms = wrap(m - dusk, 1440);
    const mid = span / 2;
    const half = trueDarkHalf(span, trueDarkMinutes);
    const darkStart = mid - half;
    const darkEnd = mid + half;
    if (ms <= darkStart && darkStart > 0) {
      const x = ms / darkStart;
      return { w: twilightEase(x), te: 1 + sink * x, x, phase: 'evening' };
    }
    if (ms >= darkEnd && span > darkEnd) {
      const x = (span - ms) / (span - darkEnd);
      return { w: twilightEase(x), te: -sink * x, x, phase: 'before dawn' };
    }
    return { w: 0, te: ms < mid ? 1 + sink : -sink, x: 1, phase: 'true dark' };
  }

  // § 4a — the true-dark window, centred on the midpoint of dusk and the next dawn. A port of
  // SUNARC.deepWindow.
  function trueDarkWindow(rise, set, tw, trueDarkMinutes) {
    const setX = set < rise ? set + 1440 : set;
    const dusk = setX + tw;
    const dawn = rise - tw;
    const span = wrap(dawn - dusk, 1440);
    const mid = dusk + span / 2;
    const half = trueDarkHalf(span, trueDarkMinutes);
    return { mid: wrap(mid, 1440), from: wrap(mid - half, 1440), to: wrap(mid + half, 1440) };
  }

  // One frame of the day, as a pure function of (W, H, minutes, sunrise, sunset, appearance),
  // spec § 10. A port of SUNARC.both() in cmo/brand/sbis/patterns/sun-arc/sunarc.js at its
  // lock. `appearance` is the owner's ('light' | 'dark'); nothing here derives it from the time.
  function frame(W, H, clockMinutes, rise, setMinutes, appearance, tokens) {
    const tw = tokens.twilightMinutes;
    // One extended minute axis (spec § 4a): a sunset after local midnight, wrapped below
    // sunrise, is carried past 1440, and a clock minute before the carried dusk is read on the
    // same axis, so the small hours belong to the day that is still ending. Done here so the
    // function is total on its inputs.
    const set = setMinutes < rise ? setMinutes + 1440 : setMinutes;
    let m = clockMinutes;
    if (set + tw > 1440 && m < set + tw - 1440) m += 1440;
    // The mirror: a sunrise before 00:30 puts dawn before local midnight, so the last minutes
    // of the clock day belong to the dawn that is starting, read on the same axis.
    else if (rise - tw < 0 && m >= rise - tw + 1440) m -= 1440;
    const t = dayProgress(m, rise, set, tw);
    const envVal = env(t, tokens.envelopeEdge);
    const night = nightAmount(m, rise, set, tw);
    const twilight = twilightWeight(m, rise, set, envVal, t, tw, tokens.trueDarkMinutes, tokens.twilightSink);
    const dark = appearance === 'dark';
    const on = t >= -0.02 && t <= 1.02;

    // § 6 — day → night across each twilight window, then night → true dark by 1 − w.
    const g = dark
      ? { day: tokens.groundDarkDay, night: tokens.groundDarkNight, deep: tokens.groundDarkDeep }
      : { day: tokens.groundLightDay, night: tokens.groundLightNight, deep: tokens.groundLightDeep };
    const ground = mixOkLab(mixOkLab(g.day, g.night, night), g.deep, night * (1 - twilight.w));

    const geom = geometry(W, H, Math.min(1, Math.max(0, t)), tokens);
    let opacity = sunOpacity({ t, env: envVal, nightAmount: night, peakOpacity: dark ? tokens.peakOpacityDark : tokens.peakOpacity });
    if (t <= 0 || t >= 1) opacity *= (1 - night);

    // § 7 — the day halo on the sun (unchanged from 09-19).
    const halo = on && night < 1
      ? { x: geom.pos.x, y: geom.pos.y, r: tokens.glowRadiusRatio * geom.R, a: tokens.glowDayAlpha * envVal * (1 - night), color: tokens.glowColor }
      : null;

    // § 7 — the twilight glow: φ² × R, centred on the sun's own circle carried past each end.
    let glow = null;
    if (twilight.w > 0.001) {
      const p = geometry(W, H, twilight.te, tokens).pos;
      const color = dark
        ? mixOkLab(tokens.glowColor, tokens.twilightDarkTo, 0.30 + 0.50 * twilight.x)
        : mixOkLab(tokens.twilightLightFrom, tokens.glowColor, 0.45 + 0.25 * twilight.x);
      glow = {
        x: p.x, y: p.y, r: tokens.twilightRadiusRatio * geom.R,
        a: (dark ? tokens.twilightAlphaDark : tokens.twilightAlphaLight) * twilight.w,
        color,
      };
    }

    return { t, env: envVal, night, twilight, on, geom, appearance: dark ? 'dark' : 'light', ground, opacity, halo, glow };
  }

  // A glow's alpha at a pixel: a → mid · a → 0 across 0 → mid-stop → 100 % of its radius.
  function glowAt(glow, x, y, tokens) {
    if (!glow) return 0;
    const f = Math.hypot(x - glow.x, y - glow.y) / glow.r;
    if (f >= 1) return 0;
    const stop = tokens.glowMidStop;
    const mid = tokens.glowMidRatio;
    return f <= stop ? glow.a * (1 - (1 - mid) * f / stop) : glow.a * mid * (1 - (f - stop) / (1 - stop));
  }

  function degToRad(deg) { return (deg * Math.PI) / 180; }
  function radToDeg(rad) { return (rad * 180) / Math.PI; }
  function wrap(v, max) { return ((v % max) + max) % max; }

  function dayOfYear(d) {
    const start = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    return Math.floor((d.getTime() - start.getTime()) / 86400000) + 1;
  }

  function noaaSunriseSunset(lat, lon, date, utcOffsetMinutes) {
    const d = date instanceof Date ? date : new Date(date);
    const N = dayOfYear(d);
    const lngHour = lon / 15;

    function calc(isRise) {
      const t = N + ((isRise ? 6 : 18) - lngHour) / 24;
      const M = 0.9856 * t - 3.289;
      const Mrad = degToRad(M);
      let L = M + 1.916 * Math.sin(Mrad) + 0.020 * Math.sin(2 * Mrad) + 282.634;
      L = wrap(L, 360);
      const Lrad = degToRad(L);

      let RA = radToDeg(Math.atan(0.91764 * Math.tan(Lrad)));
      RA = wrap(RA, 360);
      const Lquad = Math.floor(L / 90) * 90;
      const RAquad = Math.floor(RA / 90) * 90;
      RA = (RA + (Lquad - RAquad)) / 15;

      const sinDec = 0.39782 * Math.sin(Lrad);
      const cosDec = Math.cos(Math.asin(sinDec));

      const latRad = degToRad(lat);
      const cosH = (Math.cos(degToRad(90.833)) - sinDec * Math.sin(latRad)) / (cosDec * Math.cos(latRad));
      if (cosH > 1 || cosH < -1) return null;

      const H = isRise ? (360 - radToDeg(Math.acos(cosH))) / 15 : radToDeg(Math.acos(cosH)) / 15;
      const T = H + RA - 0.06571 * t - 6.622;
      const UT = wrap(T - lngHour, 24);
      const localMinutes = wrap(UT * 60 + utcOffsetMinutes, 1440);
      return localMinutes;
    }

    const rise = calc(true);
    const set = calc(false);
    if (rise === null || set === null) return null;
    return { rise, set };
  }

  // § 5 — sunrise and sunset for a point on the day `date`. In polar day or night the spec
  // says to hold the last valid pair; this finds it by walking back to the most recent day that
  // has one, so a cold load inside the polar night matches a session that ran through the polar
  // sunset. Returns null only if half a year of days all lack a sunrise, which cannot happen.
  function solarPair(coords, date, utcOffsetMinutes) {
    if (!coords) return null;
    let probe = date;
    for (let back = 0; back <= 183; back += 1) {
      const times = noaaSunriseSunset(coords.lat, coords.lon, probe, utcOffsetMinutes);
      if (times) {
        // Above about 64 degrees in midsummer the sun goes down after local midnight, so the
        // setting time wraps to a small number. Carry it into the next day so dawn precedes dusk.
        return { rise: times.rise, set: times.set <= times.rise ? times.set + 1440 : times.set };
      }
      probe = new Date(probe.getTime() - 86400000);
    }
    return null;
  }

  function parseColorToRgb(colorStr) {
    if (!colorStr || typeof colorStr !== 'string') return [0, 0, 0];
    colorStr = colorStr.trim();
    if (colorStr.startsWith('#')) {
      let hex = colorStr.slice(1);
      if (hex.length === 3) {
        hex = hex.split('').map(c => c + c).join('');
      }
      const num = parseInt(hex, 16);
      return [(num >> 16) & 255, (num >> 8) & 255, num & 255];
    }
    const match = colorStr.match(/rgba?\s*\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)/i);
    if (match) {
      return [parseFloat(match[1]), parseFloat(match[2]), parseFloat(match[3])];
    }
    if (typeof document !== 'undefined' && document.createElement) {
      const probe = document.createElement('span');
      probe.style.color = colorStr;
      document.body?.appendChild(probe);
      const computed = window.getComputedStyle ? window.getComputedStyle(probe).color : '';
      probe.remove();
      const compMatch = computed.match(/rgba?\s*\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)/i);
      if (compMatch) {
        return [parseFloat(compMatch[1]), parseFloat(compMatch[2]), parseFloat(compMatch[3])];
      }
    }
    return [0, 0, 0];
  }

  function srgbToLinear(c) {
    c = c / 255;
    return c <= 0.04045 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
  }

  function linearToSrgb(c) {
    c = Math.max(0, Math.min(1, c));
    return c <= 0.0031308 ? 12.92 * c : 1.055 * Math.pow(c, 1 / 2.4) - 0.055;
  }

  function rgbToOklab(r, g, b) {
    const lr = srgbToLinear(r);
    const lg = srgbToLinear(g);
    const lb = srgbToLinear(b);
    const l = 0.4122214708 * lr + 0.5363325363 * lg + 0.0514459929 * lb;
    const m = 0.2119034982 * lr + 0.6806995451 * lg + 0.1073969566 * lb;
    const s = 0.0883024619 * lr + 0.2817188376 * lg + 0.6299787005 * lb;
    const l_ = Math.cbrt(l);
    const m_ = Math.cbrt(m);
    const s_ = Math.cbrt(s);
    return [
      0.2104542553 * l_ + 0.7936177850 * m_ - 0.0040720468 * s_,
      1.9779984951 * l_ - 2.4285922050 * m_ + 0.4505937099 * s_,
      0.0259040371 * l_ + 0.7827717662 * m_ - 0.8086757660 * s_,
    ];
  }

  function oklabToRgb(L, a, b) {
    const l_ = L + 0.3963377774 * a + 0.2158037573 * b;
    const m_ = L - 0.1055613458 * a - 0.0638541728 * b;
    const s_ = L - 0.0894841775 * a - 1.2914855480 * b;
    const l = l_ * l_ * l_;
    const m = m_ * m_ * m_;
    const s = s_ * s_ * s_;
    const lr = +4.0767416621 * l - 3.3077115913 * m + 0.2309699292 * s;
    const lg = -1.2684380046 * l + 2.6097574011 * m - 0.3413193965 * s;
    const lb = -0.0041960863 * l - 0.7034186147 * m + 1.7076147010 * s;
    return [linearToSrgb(lr) * 255, linearToSrgb(lg) * 255, linearToSrgb(lb) * 255];
  }

  function rgbToHex(r, g, b) {
    const clamp = (v) => Math.max(0, Math.min(255, Math.round(v)));
    return '#' + [clamp(r), clamp(g), clamp(b)].map(x => x.toString(16).padStart(2, '0')).join('').toUpperCase();
  }

  function mixOkLab(color1, color2, tRaw) {
    // Clamped like the reference's A.mix, so no caller can extrapolate past either colour.
    const t = Math.max(0, Math.min(1, tRaw));
    const [r1, g1, b1] = parseColorToRgb(color1);
    const [r2, g2, b2] = parseColorToRgb(color2);
    const [L1, a1, b_1] = rgbToOklab(r1, g1, b1);
    const [L2, a2, b_2] = rgbToOklab(r2, g2, b2);
    const L = L1 + (L2 - L1) * t;
    const a = a1 + (a2 - a1) * t;
    const b = b_1 + (b_2 - b_1) * t;
    const [r, g, b_out] = oklabToRgb(L, a, b);
    return rgbToHex(r, g, b_out);
  }

  function nightGroundColor(dayGround, ink, warmDark, mixInk, mixWarm) {
    const step1 = mixOkLab(dayGround, ink, mixInk);
    const step2 = mixOkLab(step1, warmDark, mixWarm);
    return step2;
  }

  function mixedGround(dayGround, nightGround, nightAmt) {
    return mixOkLab(dayGround, nightGround, nightAmt);
  }

  function oklabLightness(colorStr) {
    const [r, g, b] = parseColorToRgb(colorStr);
    const [L] = rgbToOklab(r, g, b);
    return L;
  }

  // The media query for the owner's dark setting, or null where matchMedia is unavailable
  // (then the light row, the page's own default).
  function systemAppearanceQuery(matchMediaImpl) {
    const mm = matchMediaImpl || (typeof window !== 'undefined' && typeof window.matchMedia === 'function'
      ? window.matchMedia.bind(window) : null);
    if (!mm) return null;
    try {
      return mm('(prefers-color-scheme: dark)') || null;
    } catch (e) {
      return null;
    }
  }

  // Whether the page declares a dark appearance of its own (CSS `color-scheme` containing
  // `dark`, inherited down to the sun arc's container).
  function pageSupportsDark(el) {
    if (!el || typeof window === 'undefined' || typeof window.getComputedStyle !== 'function') return false;
    const scheme = window.getComputedStyle(el).colorScheme || '';
    return /\bdark\b/.test(scheme);
  }

  function listenQuery(query, handler, on) {
    if (!query) return;
    if (typeof query.addEventListener === 'function') {
      if (on) query.addEventListener('change', handler);
      else query.removeEventListener('change', handler);
    } else if (typeof query.addListener === 'function') {
      if (on) query.addListener(handler);
      else query.removeListener(handler);
    }
  }

  // Runtime state
  let currentInstance = null;

  function mount(root, options) {
    if (!root) return null;
    if (currentInstance) {
      currentInstance.teardown();
    }

    const opts = options || {};
    const getNow = opts.getNow || (() => new Date());
    const customSetTimeout = opts.setTimeout || setTimeout;
    const customClearTimeout = opts.clearTimeout || clearTimeout;
    const customSetInterval = opts.setInterval || setInterval;
    const customClearInterval = opts.clearInterval || clearInterval;
    const resizeDebounceMs = typeof opts.resizeDebounceMs === 'number' ? opts.resizeDebounceMs : 250;
    // § 5 rung 1 — a location the app already holds of its own accord. Convey holds none, so
    // nothing passes this; it exists so a surface that genuinely holds one can feed it in.
    // The engine asks the device for a position nowhere: § 12 accepts no location request
    // attributable to this pattern, and a granted permission is still a request.
    const heldCoords = opts.heldCoords || null;
    const getZoneId = opts.getZoneId || systemZoneId;
    // Spec §§ 2, 6, 10 (09-23): the appearance is the owner's system setting, read live from
    // `prefers-color-scheme` and followed through its change events. Never the clock. The
    // dark row is drawn only where the page itself declares dark support in CSS
    // `color-scheme`, so the ground and the page's own text always follow one source: a
    // light-only page keeps the light row at every hour instead of putting its ink on a dark
    // ground. The CSS property is what counts, not the <meta name="color-scheme"> tag.
    // Convey has a dark appearance, and this engine still follows the computed scheme.
    const appearanceQuery = systemAppearanceQuery(opts.matchMedia);
    const getAppearance = () => (
      pageSupportsDark(root) && appearanceQuery && appearanceQuery.matches ? 'dark' : 'light'
    );

    // The twilight glow sits under the day halo, and both sit under the sun (spec § 7).
    let twilightEl = root.querySelector('.sunarc-twilight');
    if (!twilightEl) {
      twilightEl = document.createElement('div');
      twilightEl.className = 'sunarc-twilight';
      twilightEl.setAttribute('aria-hidden', 'true');
      root.insertBefore(twilightEl, root.firstChild);
    }

    let glowEl = root.querySelector('.sunarc-glow');
    if (!glowEl) {
      glowEl = document.createElement('div');
      glowEl.className = 'sunarc-glow';
      glowEl.setAttribute('aria-hidden', 'true');
      root.appendChild(glowEl);
    }

    let sunEl = root.querySelector('.sunarc-sun');
    if (!sunEl) {
      sunEl = document.createElement('img');
      sunEl.className = 'sunarc-sun';
      sunEl.src = '/static/sol-status/mark.svg';
      sunEl.alt = '';
      sunEl.setAttribute('aria-hidden', 'true');
      sunEl.draggable = false;
      root.appendChild(sunEl);
    }

    // § 5 rung 3 — the fixed default, for a zone identifier the tzdb table does not know.
    const FALLBACK_SUN_TIMES = { rise: 6 * 60 + 30, set: 19 * 60 + 30 };
    let currentSunTimes = null;
    let currentZoneId = null;
    let currentDateKey = null;
    let currentFrame = null;
    let intervalId = null;
    let resizeTimer = null;

    function dateKeyOf(d) {
      return d.getFullYear() + '-' + d.getMonth() + '-' + d.getDate();
    }

    function paintGlow(el, glow, tokens) {
      if (!glow || glow.a <= 0.002) {
        el.style.background = 'none';
        return;
      }
      el.style.width = (2 * glow.r) + 'px';
      el.style.height = (2 * glow.r) + 'px';
      el.style.left = (glow.x - glow.r) + 'px';
      el.style.top = (glow.y - glow.r) + 'px';
      const [gr, gg, gb] = parseColorToRgb(glow.color);
      const a0 = glow.a;
      const aMid = glow.a * tokens.glowMidRatio;
      const stopPct = (tokens.glowMidStop * 100).toFixed(1) + '%';
      // closest-side: the gradient's 100 % stop is the element's own edge. The bare `circle`
      // default is farthest-corner, which left alpha at the border-radius clip, a hard edge.
      el.style.background = `radial-gradient(circle closest-side, rgba(${gr},${gg},${gb},${a0}) 0%, rgba(${gr},${gg},${gb},${aMid}) ${stopPct}, rgba(${gr},${gg},${gb},0) 100%)`;
    }

    function recompute() {
      const tokens = parseTokens(root);
      if (!tokensValid(tokens)) {
        root.style.backgroundColor = 'transparent';
        twilightEl.style.display = 'none';
        glowEl.style.display = 'none';
        sunEl.style.display = 'none';
        currentFrame = null;
        return;
      }

      twilightEl.style.display = '';
      glowEl.style.display = '';
      sunEl.style.display = '';

      const now = getNow();
      const clock = now.getHours() * 60 + now.getMinutes() + now.getSeconds() / 60;

      // § 5: "Recompute once a day and on a timezone or location change."
      const zoneId = getZoneId();
      const dateKey = dateKeyOf(now);
      if (!currentSunTimes || zoneId !== currentZoneId || dateKey !== currentDateKey) {
        const offset = typeof opts.utcOffsetMinutes === 'number' ? opts.utcOffsetMinutes : -now.getTimezoneOffset();
        currentSunTimes = solarPair(heldCoords || zonePoint(zoneId), now, offset) || FALLBACK_SUN_TIMES;
        currentZoneId = zoneId;
        currentDateKey = dateKey;
      }

      const rise = currentSunTimes.rise;
      const set = currentSunTimes.set;

      const W = window.innerWidth || (document.documentElement ? document.documentElement.clientWidth : 1280);
      const H = window.innerHeight || (document.documentElement ? document.documentElement.clientHeight : 820);

      // The owner's appearance, read from the system on every paint. The pattern only reads it;
      // it never writes an appearance, a theme or a data attribute anywhere (spec §§ 6, 10).
      const f = frame(W, H, clock, rise, set, getAppearance(), tokens);
      currentFrame = f;
      root.style.backgroundColor = f.ground;

      sunEl.style.width = f.geom.diameter + 'px';
      sunEl.style.height = f.geom.diameter + 'px';
      sunEl.style.left = (f.geom.pos.x - f.geom.R) + 'px';
      sunEl.style.top = (f.geom.pos.y - f.geom.R) + 'px';
      sunEl.style.opacity = f.on ? String(f.opacity) : '0';

      paintGlow(twilightEl, f.glow, tokens);
      paintGlow(glowEl, f.halo, tokens);
    }

    function onResize() {
      if (resizeTimer) {
        customClearTimeout(resizeTimer);
      }
      resizeTimer = customSetTimeout(() => {
        resizeTimer = null;
        recompute();
      }, resizeDebounceMs);
    }

    function onVisibilityChange() {
      if (typeof document !== 'undefined' && document.visibilityState === 'visible') {
        recompute();
      }
    }

    // Initial paint
    recompute();

    intervalId = customSetInterval(recompute, 60000);

    if (typeof window !== 'undefined' && typeof window.addEventListener === 'function') {
      window.addEventListener('resize', onResize);
    }
    if (typeof document !== 'undefined' && typeof document.addEventListener === 'function') {
      document.addEventListener('visibilitychange', onVisibilityChange);
    }
    // The owner flips their setting: repaint now rather than at the next minute.
    listenQuery(appearanceQuery, recompute, true);

    function teardown() {
      if (intervalId) {
        customClearInterval(intervalId);
        intervalId = null;
      }
      if (resizeTimer) {
        customClearTimeout(resizeTimer);
        resizeTimer = null;
      }
      if (typeof window !== 'undefined' && typeof window.removeEventListener === 'function') {
        window.removeEventListener('resize', onResize);
      }
      if (typeof document !== 'undefined' && typeof document.removeEventListener === 'function') {
        document.removeEventListener('visibilitychange', onVisibilityChange);
      }
      listenQuery(appearanceQuery, recompute, false);
      if (currentInstance === instance) {
        currentInstance = null;
      }
    }

    const instance = {
      recompute,
      teardown,
      getHeldCoords: () => heldCoords,
      getZoneId: () => currentZoneId,
      getSunTimes: () => currentSunTimes,
      getAppearance,
      getFrame: () => currentFrame,
    };
    currentInstance = instance;
    return instance;
  }

  function recompute() {
    if (currentInstance) {
      currentInstance.recompute();
    }
  }

  function teardown() {
    if (currentInstance) {
      currentInstance.teardown();
    }
  }

  const SunArc = {
    parseNumeric,
    validateColor,
    parseTokens,
    tokensValid,
    geometry,
    env,
    nightAmount,
    dayProgress,
    sunOpacity,
    twilightWeight,
    trueDarkHalf,
    trueDarkWindow,
    frame,
    glowAt,
    pageSupportsDark,
    noaaSunriseSunset,
    solarPair,
    parseZoneTable,
    zonePoint,
    systemZoneId,
    zoneCount: SUNARC_ZONE_COUNT,
    srgbToLinear,
    linearToSrgb,
    rgbToOklab,
    oklabToRgb,
    rgbToHex,
    mixOkLab,
    nightGroundColor,
    mixedGround,
    oklabLightness,
    mount,
    recompute,
    teardown,
  };

  if (typeof window !== 'undefined') {
    window.SunArc = SunArc;
  }

  // Auto-mount if #sunarc exists at script load
  if (typeof document !== 'undefined') {
    const el = document.getElementById('sunarc');
    if (el) {
      mount(el);
    }
  }
})();
