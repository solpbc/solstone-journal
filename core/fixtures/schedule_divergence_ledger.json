{
  "schema": "schedule-divergence-ledger/1",
  "entries": [
    {
      "id": "AF-S2A-001-config-read-errors",
      "native_behavior": "Malformed, non-object, and unreadable schedules config exits 1 with a diagnostic.",
      "reference_behavior": "The Python CLI prints a config read error and returns exit 0."
    },
    {
      "id": "AF-S2A-002-state-read-errors",
      "native_behavior": "Malformed, non-object, and unreadable scheduler state exits 1 and renders affected LAST RUN and NEXT DUE as invalid.",
      "reference_behavior": "The Python CLI silently treats unreadable scheduler state as empty."
    }
  ]
}
