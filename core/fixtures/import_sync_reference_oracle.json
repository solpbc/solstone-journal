{
  "schema": "w9-w10-oracles/1",
  "provenance": {
    "captured_from_rev": "86fd678a6b3aec2eb4f33a4c934f0cf34a099542",
    "python": "3.13.14",
    "method": "read the reference sub-CLI's real parser construction and the sync registry's live objects"
  },
  "sync": {
    "registry_ordered": [
      [
        "plaud",
        "solstone.think.importers.plaud"
      ],
      [
        "obsidian",
        "solstone.think.importers.obsidian"
      ],
      [
        "audio",
        "solstone.think.importers.audio"
      ]
    ],
    "backends": [
      {
        "name": "plaud",
        "sync_signature": "(journal_root: pathlib._local.Path, *, dry_run: bool = True) -> dict[str, typing.Any]"
      },
      {
        "name": "obsidian",
        "sync_signature": "(journal_root: pathlib._local.Path, *, dry_run: bool = True, source_path: pathlib._local.Path | None = None, force: bool = False) -> dict[str, typing.Any]"
      },
      {
        "name": "audio",
        "sync_signature": "(journal_root: pathlib._local.Path, *, dry_run: bool = True, source_path: pathlib._local.Path | None = None, force: bool = False, auto: bool | str = True) -> dict[str, typing.Any]"
      }
    ],
    "state_path_shape": "<journal>/imports/<backend>.json, written atomically with the private import file mode",
    "default_is_preview": "--sync shows status without downloading; --save is what makes it act"
  }
}
